services:
  - type: web
    name: ujala-portfolio
    runtime: static
    plan: free
    buildCommand: "npm run build"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
